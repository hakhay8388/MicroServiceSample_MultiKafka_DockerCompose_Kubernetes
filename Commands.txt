
kubectl delete --all pods
kubectl delete --all pods --namespace=foo


kubectl delete --all deployments
kubectl delete --all deployments --namespace=foo

kubectl delete --all namespaces

# docker compose file dan kubernate dosyaları oluşturmak için
kompose convert -f docker-compose_for_KubernateConvert.yml

# docker compose file dan kubernate dosyası oluşturmak için (tek dosya)
kompose convert -f docker-compose_for_KubernateConvert.yml -o simple.yaml

# butun servisleri ve deploymentları oluştur
kubectl create -f .


kubectl.exe get svc
kubectl.exe get po

# Full delete
kubectl delete daemonsets,replicasets,services,deployments,pods,rc,ingress --all --all-namespaces


kubectl apply -f https://raw.githubusercontent.com/kubernetes/dashboard/v2.4.0/aio/deploy/recommended.yaml


skaffold.exe init --compose-file docker-compose_kebernetes.yml

skaffold run

skaffold run --port-forward

https://skaffold.dev/docs/pipeline-stages/port-forwarding/

skaffold delete

kubectl port-forward service/kafka1 9092:9092 service/kafka2 9093:9093 service/kafka3 9094:9094


kubectl patch svc kafka1 -p '{"spec":{"externalIPs":["192.168.0.194"]}}'

helm install aaaaa  stable/nginx-ingress -n kafkanamespace \
  --set controller.ingressClass=kafka \
  --set tcp.9093="operator/kafka-bootstrap:9092" \
  --set tcp.9095="operator/kafka1-service:9092" \
  --set tcp.9097="operator/kafka2-service:9093" \
  --set tcp.9099="operator/kafka3-service:9094"
